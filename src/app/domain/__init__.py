"""Domain layer: entities, business rules, and ports.

This is the heart of the application. It must NOT import from the
`application`, `infrastructure`, or `api` layers (dependency rule:
arrows always point inwards).
"""
